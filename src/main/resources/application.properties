spring.application.name=cardapio-api

# MySQL Configuration
#spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
spring.datasource.url=jdbc:mysql://10.0.1.75:3306/gpp_fulano?zeroDateTimeBehavior=convertToNull
spring.datasource.username=${DB_USER:g3_informatica}
spring.datasource.password=${DB_PASSWORD:#g31nf#}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Server Port
server.port=8081

# App Configuration
app.config.view-only=${VIEW_ONLY:false}
